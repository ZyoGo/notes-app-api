// !badNotePayloads || badNotePayloads.length === 0;

const badNotePayloads = '1';

if (badNotePayloads || badNotePayloads.length === 1) {
  console.log('true');
} else {
  console.log('false');
}

// checkLogicalNot();

console.log(badNotePayloads);
